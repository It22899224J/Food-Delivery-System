apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: delivery-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delivery-db
  template:
    metadata:
      labels:
        app: delivery-db
    spec:
      containers:
      - name: postgres
        image: postgres:15
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "password"
        - name: POSTGRES_DB
          value: "delivery"
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: delivery-db-storage
      volumes:
      - name: delivery-db-storage
        persistentVolumeClaim:
          claimName: delivery-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-db
spec:
  selector:
    app: delivery-db
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
